# 实施计划索引

每期一份，把下方路线基线里的一行展开成**可执行的步骤与验收标准**。总体架构回答「系统是什么样」，这里回答「怎么做、做到什么程度算完」。

| 文档 | 这一期回答什么 | 状态 |
|---|---|---|
| [P0-plan.md](./P0-plan.md) | agent 好不好用 —— 教师提问，agent 自己写 Python、跑、出图 | 已完成（2026-08-03，验收四条全过） |
| [P1-plan.md](./P1-plan.md) | 不可信代码跑在这台机器上安不安全 —— gVisor、配额、broker 拆分 | 已完成（2026-08-07，验收六条全过） |
| [P2-plan.md](./P2-plan.md) | 这台机器上的东西挂了会怎样 —— Postgres、Redis、worker 拆分 | 已完成（2026-08-08，验收六条全过）。租约记名那笔债**已由 P3 步骤七偿还** |
| [P3-plan.md](./P3-plan.md) | 谁在用、能用多少、人能不能插手 —— 认证隔离、配额限流、取消与 HITL、幂等键 | 已完成（2026-08-08，验收七条全过）。租约记名这笔 P1 遗债已偿还。**本期改的接口打穿了三个历史验收脚本**（[§8.5](./P3-plan.md)），**两条验收判据本身不成立**（[§8.7](./P3-plan.md)） |
| [P4-plan.md](./P4-plan.md) | 出了问题怎么查、花了多少钱、产物放哪 —— 可观测性、MinIO 产物存储、成本看板 | 已完成（2026-08-10，**验收八条全过**，[§8.14](./P4-plan.md)），但**三项交付物已被后续撤除**：产物存储（`0009`，2026-08-12）、可观测性与 MinIO（2026-08-13，见下）。**留下的是成本看板与 nginx 直发**，`p4.sh` 相应从六条减为四条。那次全量重跑照出的三个真因（含**磁盘配额从 P1 起就没生效过**）与四条只会绿的判据（[§8.13](./P4-plan.md)）**仍然有效** —— 修的是别处的代码。**`ANALYSIS_TOKEN` 的校准仍挂着**，见 §8.14 |
| [P5-plan.md](./P5-plan.md) | 前端能不能开工 —— 账号与课题组、工作目录浏览、会话增删改查与聊天历史 | 已完成（2026-08-11，五个提交，1059 条用例全绿）。**唯一一期先落地后补文档**（[§1.1](./P5-plan.md)），代价是期内自我推翻了一次。**「没有 `p5.sh`」这笔债已于 2026-08-14 还清**（[§4.1](./P5-plan.md)）：`P5①`–`P5④` 补进 `verify.sh`，一次跑过、32 条断言全绿，本期改到的三处**都没有打穿历史判据** |
| [**P6-decision.md**](./P6-decision.md) | **不是实施计划，是它们的前置** —— 智能体配置与平台能力目录开工前必须拍板的 **68 条决策**，每条给选项、代价与定案 | 已定案（2026-08-13）。**19 条与初稿建议相反**，各自记了接受的代价；**三处组合风险**与**四处「必须实测」**已标出。它推翻了三份上游文档的结论，修订同日完成（见下） |

| [P6-plan.md](./P6-plan.md) | 配置能不能真的影响行为 —— 前端底座（真登录 / API 层 / SSE hook / HITL UI）与提示词生效 | **开发完成，主判据通过**（2026-08-14）。默认全量验收中 `P6①`～`P6③` 全过；双端门禁与覆盖率全绿。仍保留部分浏览器人工观察项；整份回归脚本因旧阶段 `P2①` 未验而退出 2（[§8](./P6-plan.md)） |
| [P7-plan.md](./P7-plan.md) | 共享是不是真的共享、审核是不是真的闸门 —— `agent` 表与版本序列、三档可见性、`reviewer` 与审核后台 | **开发完成，两条主判据通过**（2026-08-14）。开工前三处拍板全部落地：引用生效纳入本期、场景两页按 A1/J4 合并掉、引入 playwright 只覆盖可见性主链路。六条判据，总数 29 → **35**；双端门禁与覆盖率全绿。**`P7⑥` 先红过两次才写成现在这样**，且把仓储层一处过滤条件去掉之后它真的红（[§8](./P7-plan.md)）。**收尾跑了一轮不带任何开关的全量回归：35 条全过、退出码 0**（[§8.6](./P7-plan.md)）|
| [P8-plan.md](./P8-plan.md) | 别人写的东西进沙箱之后算不算数 —— skill 的上传、八条校验、宿主机存储、开跑前全量对齐、`SkillsMiddleware` 接上 | **开发完成，两条主判据都成立**（2026-08-15）。九个步骤逐步提交；判据总数 35 → **41**，双端门禁全绿（后端 1122、前端 123）。**开工前三次探针推翻了 [H2](./P6-decision.md) 的一句话**：skill 并不像那里写的那样自动热更新，中间件的加载被 checkpoint 里的 `skills_metadata` 短路掉（[§2.1](./P8-plan.md)）。收尾按实测把 `.html` 纳入附件白名单 |
| [P9-plan.md](./P9-plan.md) | 一次分析里跑着不止一个 agent 时，那些「一个 run 一个 agent」的假设还成不成立 —— 子智能体的装配、事件路径、token 归属、嵌套中断、递归上限 | **开发完成**（2026-08-15，九个步骤逐步提交）。判据总数 41 → **47**。开工前三处拍板：agent 自带子智能体纳入本期（「场景」由此落地）、事件 `path` 映射成子智能体名、验收保留账单对账与 playwright 第三条链路。**开工前五处实测全部跑完，两处推翻了决策文档的预设**（[§2](./P9-plan.md)）：`ns` 里没有子智能体的名字（G6）、子图的 `recursion_limit` 是硬编码的 9999 而不是平台设的 60（G9）。另外三处是好消息：子图 token 带得上来（G7）、嵌套 HITL 两半都通（G8，退路用不上）、子智能体列表没有 P8 那种 state 短路。**收尾时账单对账与依赖真实服务的判据记「未验」**（[§8](./P9-plan.md)），不以 skip 冒充通过 |
| [P10-plan.md](./P10-plan.md) | 一次分析要用的东西第一次不在平台手里 —— 外网 MCP 目录、申请与放行、超时熔断、撞名剔除、外发标注 | **开发完成，两条主判据都成立**（2026-08-16，九个步骤逐步提交）。判据总数 47 → **54**，双端门禁全绿（后端 1224、前端 142）。**收尾全量回归跑了两轮：第一轮 53 过 1 未验**（`P1①` 要交互式 sudo），**第二轮在有 tty 的终端里 54 条全过、退出码 0**（[§8.6](./P10-plan.md)）——**P0–P9 那 47 条一条都没被打穿**，含历来最容易偶发的 `P2①` 与要 root 的破坏性四条。开工前三处拍板：申请流程纳入本期并复用 `reviews`（审批人限管理员）、熔断只数传输层失败、验收以本地夹具为主并保留一条真外网判据。**开工前九处实测全部跑完，三处推翻了决策文档的预设**（[§2](./P10-plan.md)）：外部工具会**静默顶掉**内置的 `read_file`（F6 以为只是「调哪个由注册顺序决定」）、30 秒超时的落点错了（connection 的 `timeout` 管不住 hang 住的服务，实测等 **300.05 秒**）、F1 那句「全平台卡 30 秒」对 SSE / HTTP 不成立。另有一处是决策文档从未设想过的失效形态：**一个坏服务会让整批工具加载失败**。**部署之后又冒出两处门禁看不见的**（[§8.3](./P10-plan.md)）：一次 run 装配了两遍因而连了两次 MCP、api 容器缺 `extra_hosts`；**验收脚本本身还骗过一次**（[§8.4](./P10-plan.md)：端口被占，而红的地方指向熔断） |

| [P11-plan.md](./P11-plan.md) | 前端最后六处假数据背后缺的后端能力 —— 用量归属、账号字段补全、沙箱池状态、场景与智能体分流 | **开发完成，主判据成立**（2026-08-16，十一个提交）。判据总数 54 → **61**，双端门禁全绿（后端 1254、前端 142）。**主判据是部署之后实测的**：修复前按 `userId` 只捞得到 1 条 `CHAIN`、token 为 0；修复后 `GENERATION` **2 条、7412 token**，`/api/usage/me` 报同一个数。**开工前六处实测，四处推翻了 2026-08-13 的用量决策**（[§2.1–§2.3](./P11-plan.md)）：Langfuse 当时**答不出「谁花了多少」**（7,118,137 个 token 全记在 `userId=null` 上，按用户切出来每人都是 0，而接口一路返回 200，真因是身份只挂在根 span 上）、v1 metrics 与 traces / sessions 三个端点在 v4 `events_only` 模式下 404（而它们的文档还活着）、费用恒为 0 的真因是**内置 100 个模型价格里没有 deepseek**（可注册）。**另外推翻了两处本计划自己写的东西**：`kind` 字段不该加（[§2.4](./P11-plan.md)，[G2](./P6-decision.md) 早已论证过判据是「有没有挂子智能体」这个客观事实，加字段等于造第二个真相源）、§1.4 的「同量级」不成立（[§8.7](./P11-plan.md)，同一次 run 上两份账差 51 倍，因 `runs` 表按定义不计 cache 命中，判据改成一个必然成立的不等式）。**部署之后冒出一处门禁看不见的**（[§8.6](./P11-plan.md)）：api 容器里 `LANGFUSE_BASE_URL` 填的是 `localhost`，两个用量端点一律 `available: false` —— 而单测里 `platform.langfuse` 本就是 `None`，走的正是同一支，因此门禁全绿。**打穿了四条 playwright 判据**（登录框 placeholder 改了）与 `verify.sh` 的造号 SQL（`email` 变 NOT NULL），均已修 |

| [P12-plan.md](./P12-plan.md) | agent 缺库时能不能自己装上 —— 沙箱出网、`pip` 装进 workspace、提示词同步 | **开发完成，主判据通过**（2026-08-18）。判据总数 61 → **62**，双端门禁全绿。推翻 [P1 §2.2](./P1-plan.md) 的零出网定案（[ADR-0017](../01design/adr/0017-sandbox-network-and-package-install.md)），但**加固清单一条未动** —— P1 说「要支持装包就得放松加固清单」只对了一半，装到 workspace 就不必（[§2.1](./P12-plan.md)）。开工前五处实测，一处抓出会静默失效的配置：**uv 完全忽略 `PIP_INDEX_URL`**，而少给一个变量的症状只是慢不报错（[§2.3](./P12-plan.md)）；**第一版探针用 `--dry-run` 对比，两个变量都「成功」，分辨不出真相**。**部署之后照出判据本身是坏的**（[§7](./P12-plan.md)）：漏了申请沙箱那一步，`execute` 报「没有运行中的沙箱」，而那个红看着像装包失败 |
| [**P13-decision.md**](./P13-decision.md) | **不是实施计划，是它们的前置** —— 智能体本体优化（缓存、压缩、提示词、记忆、三个工具、评估集）开工前的 **24 条决策**，并把它切成 P13–P15 三期 | 已定案（2026-08-19，**v0.7**）。**两条与优化点清单原文相反**：压缩机制已经装着（改为探真实上限并钉死阈值）、`web_search` 走 P10 的 MCP 目录不新建内置工具。**v0.1 把「状态栏」读成前端展示判掉了，v0.2 更正**：它是尾部注入中间件，与用户信息、system-reminder 合成同一个注入点（A8）。开工前六处实测已跑完，**两处推翻了清单的前提**，一处照出**撞递归上限现在记成 `INTERNAL`、根本分不出来**；**另有三处必须在开工前实测**，其中判分方差直接决定 P15 的验收标准长什么样。**v0.3 重制了 C 组、v0.4 纠正了其中一条**（2026-08-19）：那三处里的第 3 条已跑完，**但第一轮测错了** —— 只打了 v3 遗留端点就断定「分数写得进读不回」。按本机 OpenAPI spec 重测：`/v3/scores`、`/v2/observations`、`/v2/metrics`、`/experiments`、`/experiment-items` **全部可用**，404 响应里本来就带着 `replacement` 字段指路；真正取消的只有 trace 与 session 的读取。据此 C6 改成停用↔替代对照表、C3 整张指标表换掉、新增 C7（pairwise 留到 P15，基线输出落盘由「必需」降为「便利」）。另测出**平台内置 LLM-as-judge 可用**。**v0.5 依据《深入理解 AI Agent》第 2 章又纠正了一处、补了一条**：A8 那段「块下一轮就被换掉，所以从不参与公共前缀」是错的 —— 它正是书 2.6.4 的「每轮替换」，会让状态消息位置之后的缓存失效，定案改为**持久追加**；**主判据同时由 `cache_hit_rate` 换成 `cost_yuan`**（命中率是会假过的比值：首轮据它判出「尾部注入有害」，按单价复算 9 题里 7 题成本反而降）；新增 **A9 工具结果预算控制**（书 2.7.4 第一层，`FilesystemMiddleware` 的阈值默认 20000 从未触发过，而首轮最贵那题烧掉 12.5 万未命中 token）。**v0.7 再降级 A9**：机制修好之后第二轮实测，正常阈值下 30 次分析触发 **0 次**，而真正的成本大头是**双峰**（同题副本差 20–30 倍，5/30 炸掉、占 60% 的钱），炸掉那几次单条工具结果最大只有 1820 字符 —— **卸载在任何阈值下都拦不住一次**，A9「单项就能动一个数量级」那句判断按实测不成立（定案不撤，但不再是主要杠杆）。**v0.6 改正 A9 的诊断**：定案（降阈值）不变，但阈值降到下限 100 仍然一次都不触发 —— 真因是**落点**被算成 `/large_tool_results`，过不了平台「一切在 `/workspace` 下」那道闸，而**写盘被驳回是静默的**；同一个 bug 打中压缩的归档，26 次压缩、26 次 `file_path` 为空，折掉的原文没有留底。**C 组已落地**：10 题评估集与 runner 均已跑通（[`script/eval/`](../../script/eval/)，数据集 `zuel-finance-eval-v1`） |

| [P13-plan.md](./P13-plan.md) | 提示词改完拿什么证明它变好了 —— 评估尺子（10 题 + 指标 + runner）、上下文上限与压缩阈值、缓存友好、尾部注入 | **十三步全部走完，而主判据的结论是「分辨不出」**（2026-08-19）。三方合计 2.5404（基线）→ 1.4182（步骤七）→ 1.9463 元（第二轮），**但噪声带是 43.3%，两个差值都在带内** —— 按本期自己立的规矩，既不算改进也不算回归。**P13⑦ 两项记「未验」**：正常阈值下卸载 0/30、压缩 0/30。**最值钱的产出是一个否定结论**：成本是双峰的（同题副本差 20–30 倍，5/30 炸掉、占 60% 的钱），而**炸掉那几次的单条工具结果最大只有 1820 字符 —— A9 的前提被证伪，卸载拦不住其中任何一次**；工具参数、子图、推理量三条也逐一排除，**真因未查清，下一步要逐次模型调用的 token 埋点**。判据总数 62 → **64**（P13①② 进 `verify.sh`，P13③ 由 P13② 蕴含、P13④ 归单测）。**尺子先立起来再改被测物**：步骤一～四补错误码、补压缩采集通路、写 runner、跑基线，步骤五之后才动 agent 本体。**开工前的探针推翻了决策文档的怀疑**：`deepseek-v4-pro` 真吃得下百万上下文，压缩从未生效的真因是「85 万那条线在真实使用里够不着」。**最有价值的产出是一个负面结论**：尾部注入让 10 题里 8 题 cache 命中率下降、未命中 token 涨 20%–200%，**A6/A8 当初「挂在最后一条消息之后，缓存前缀完整保留」这个假设不成立** —— 块每轮都变，下一轮的历史在同一位置换成别的内容，缓存匹配到那儿就断。按数据改成按需注入后，E09 从 0.717 回到 0.963、E10 回到 0.960。**噪声带量出来了**：cache 命中率极差中位数 **0.059**、判分极差平均 **0.40 分**。**尺子自己错过三次**（批准漏了第二个待确认调用、`probe_coverage` 空集报 0、多轮题拿第一轮提问配最后一轮答复），三次都是「看着像被测对象错了」。收尾全量回归 62 过 1 未过（P3① 偶发，重跑三次全停住）1 未验（破坏性组要 root）。开工前六处实测已跑完，**两处推翻决策文档的预设**：`append_prompt_caching_middleware` 只装 Anthropic / Bedrock / Fireworks，**DeepSeek 一个都不沾**（A2 从「装个中间件」变成「审计前缀里有没有每轮都变的内容」）、压缩阈值可以按中间件名替换钉死（`_apply_custom_middleware` 同名原地替换）。开工前三处拍板：专用评估账号、首轮不接 judge、新增 `EventType.COMPACTION`。判据 62 → **66**（另有四条要真实 LLM 费用的不进门禁）。**顺序上有一条不能颠倒**：尺子必须先于被测物，步骤一到四做尺子并量基线，步骤五之后才动 agent 本体。**第二轮之后又加开两步**：步骤十–十一（A9 工具结果预算、A8 改持久追加）、步骤十三（**修卸载与压缩归档的落点** —— 步骤十那句「✅ 完成」是假的，机制配好了却一次都没成功过，因为落点过不了路径闸而写盘被驳回是静默的；修完真机复验卸载 0 → 3 次、压缩留底 0/26 → 12/12） |

| [P14-plan.md](./P14-plan.md) | 给 agent 一张清单和一张嘴 —— `ask_user_question` 复用 HITL、`todo_write` 与事件映射、状态栏的任务进度一节 | **开发中**（2026-08-19，九步已走完，收尾在跑）。**开工前五处实测已跑完**（[§2](./P14-plan.md)），**一处照出平台既有的一个洞**：`respond` 决策不带 `message` 时平台放行、恢复那一刻 `KeyError`，run 记成 `INTERNAL` —— 而本期的 `ask_user_question` 正是第一个只允许 `respond` 的工具，任何脚本客户端第一次调用就会踩到，它因此排成步骤一。另量到上游那份清单能力自带 **5243 字符的英文提示词**（平台自己的系统提示词才 644），静态注入不打缓存但**会让 P13 的成本基线不再可比**，收尾要重跑一轮当作 P15 的基线。四处拍板：清单只装主图、提问工具主图子图都装、进度节只报读数不复述清单、提问与审批按 `tool_name` 分流而不加字段。判据 64 → **68**，**本期是三期里唯一必须跑全量的** |

### P6–P10：智能体配置与能力目录（2026-08-13 排期，P6 计划已于 2026-08-14 写就）

需求是六条：上传 skill 并控制共享、自定义系统提示词、配置 MCP、为一次 run 挂子智能体；平台侧上传智能体、场景智能体。定案见 [P6-decision.md](./P6-decision.md)，切成五期的理由是**每期都要有一个能干净说出口的验收标准**：

| 期 | 内容 | 验收标准（一句话） |
|---|---|---|
| **P6** | 前端底座 + 提示词生效：API 层 + TanStack Query + SSE hook + HITL 审批 UI + Chat 真联调 | **配与不配自定义提示词，同一个问题的输出可见地不同** |
| **P7** | `agent` 表与版本序列 + CRUD + 三档可见性 + `reviewer` 角色 + 审核后台 | **组内共享的东西同组人看得见、别组看不见；未过审的进不了广场** |
| **P8** | skill 全链路：上传 → 七条校验 → 宿主机存储 → 开跑前全量对齐 → `SkillsMiddleware` | **挂与不挂同一个 skill，产物里看得出差别** |
| **P9** | 子智能体：一层、最多 5 个、共享沙箱、主模型 + **四处实测** | **嵌套事件带着真实 `path` 出现在前端；子智能体烧的 token 对得上账单** |
| **P10** | MCP：外网目录 + 勾选 + 超时熔断 + 外发标注 | **外网工具真被调用；连续失败 5 次真的自动停用** —— 两条都已成立（2026-08-16） |

**P9 是最不确定的一期**，四处实测（嵌套事件的真实 `ns` 形状、子图 token 是否带得上来、嵌套中断能否被 `aget_state` 查到、子图 `recursion_limit` 是各自计数还是共享父图）任一处结果为否都会改变那一期的形状。**它单独成期正是因为这个。**

> **这四处已于 2026-08-15 在开工前全部实测完成**（外加 P8 §7 第 8 条移交的第五处），结论见 [P9 §2](./P9-plan.md)。
> **两处推翻了本文档的预设**：G6 的 `ns` 里没有子智能体的名字（冒号前是节点名 `tools`），
> G9 的子图额度是框架硬编码的 9999 而不是平台设的 60 —— 后者实测跑飞会烧掉 5000 次主模型调用。
> **P9 的形状因此在开工前就定死了**，这一期不再是「最不确定的」。

**验收脚本改形态**：不再一期一个，`p1.sh`–`p4.sh` 六个级联脚本整合成一个 `verify.sh` 默认全跑（保留 `SKIP_LLM` / `SKIP_HOSTILE` 等跳过开关）。**整合是一次重构，而被重构的东西是验收过的 —— 整合完必须原样重跑一遍确认没打穿**（[P3 §8.5](./P3-plan.md) 记着「本期改的接口打穿了三个历史验收脚本」，正是这类改动的形状）。

> **整合已于 2026-08-13 提前做掉**（在 P6 开工之前），落地为 [`script/test/verify.sh`](../../script/test/verify.sh)，当时 22 条判据（2026-08-14 补进 P5 那四条之后是 26 条）。
>
> **「必须原样重跑一遍」已完成，跑了两轮**（2026-08-14，XFS prjquota 正常挂载、DeepSeek 充值之后）：
>
> **第一轮 20 过 2 未验**：过的是 P0①–⑤、P1③④⑤⑥、P2②③⑥、P3①②③④⑤⑥、P4②⑦，含一次完整的真实分析（`input_cache_read=52480 / input_uncached=117464 / output=6036`，产物 `industry_annualized_volatility.png` 57858 字节）与四种审批决策各走一遍。两条未验：
>
> - ⚠️ **P2①**：run 跑到 succeeded，但那一刀**没砍到**（`run.started` 只有 1 条，即 kill 生效前 run 已跑完）。**没触发到要测的场景，既不记通过也不记失败** —— 这正是 [P2 §8.4](./P2-plan.md) 立的规矩。
> - ⚠️ **P1①**：破坏性四条要交互式 sudo，跑不进无 tty 的会话。
>
> **第二轮 22 条全过，退出码 0**（在有 tty 的终端里跑，两条未验各自补上）：
>
> - ✅ **P2① 这一轮真砍到了**：`run.started` 2 条，且崩前已完成的工具一次都没重跑。**观察项：工具调用 4 次、返回 4 次，差值 0** —— 与第一轮量到的一样，是 [P2 §7](./P2-plan.md)「幂等键先量后定」那项的输入。
> - ✅ **P1① 四条破坏性全过**：死循环被 `--cpus=1` 限住（100.91%）、fork 炸弹被 `--pids-limit=128` 挡住（宿主进程 640 → 647）、`/workspace` 在 5120MB 处 ENOSPC 且容器重建后配额仍在、`/tmp` 在 512m 处 ENOSPC。宿主机内存、磁盘、进程数都回到基线。
> - 这一轮的真实分析产物是 `outputs/行业市值占比与年化波动率.png`（108013 字节），token `34688 / 98919 / 5262`。
>
> **整合本身没打穿任何东西。** 跑的过程中另抓到两个与整合无关的问题：`SANDBOX_QUOTA_DEVICE` 在重建 broker 时丢失（从 P1 起就在，见 [P6 §L2](./P6-decision.md)）、以及 `compose stop worker` 不给 `-t` 会静默干等 30 分钟。
>
> **P5 欠的四条判据已补**（2026-08-14，P6 开工前的最后一件事，见 [P5 §4](./P5-plan.md)）：`P5①`–`P5④` 加在 p4 组之后，全部免费、不调模型。判据总数 22 → **26**，免费的那批 14 → **18**。
>
> **P7 收尾跑的那一轮是 35 条第一次一起绿**（2026-08-14，**退出码 0**，见 [P7 §8.6](./P7-plan.md)）。
> 此前每次全量都因 `SKIP_LLM` / `SKIP_HOSTILE` 或某条没触发到而退 2，付费判据靠手工单跑补 ——
> 那种补法**证明不了它们在脚本里跑得起来**。这一轮同时把三条观察项攒上：`ANALYSIS_TOKEN`
> 第 14 个样本（未命中 107,780）、`P2①` 第二次真砍到且差值仍为 0、`P3①` 四次审批探针的量级。

**上游文档修订（2026-08-13 同日完成）**：[接入规范](../01design/04extension-integration.md)（形式 C 改为外网、13 项交付物缩为 4 项、§6/§7 全部关闭）、[ADR-0010](../01design/adr/0010-self-hosted-accounts-rbac.md)（新增 `reviewer`）、[安全设计 §7.2.1](../01design/07security-design.md)（角色表 + 三档可见性）、[智能体设计](../01design/03agent-design.md)（子 agent / skill / MCP 的能力开关与期次）、[风险登记 §10.2.1](../01design/09risk-register.md)（主动接受的四条风险）、[总体架构 §7](../01design/01architecture.md)（重估条件已触发）、[数据设计](../01design/06data-design.md)（`agent_config` 与组内共享）。

## 路线基线

这张表原为总体架构 §11。拆分后移到实施计划索引，保留各期最初承诺与验收口径；具体定案和最终结果以上方各期计划为准。

| 阶段 | 内容 | 验证标准 |
|---|---|---|
| **P0** | FastAPI 内运行 DeepAgents、裸 Docker 沙箱、SSE 流式输出 | 用户能对话；Agent 能写 Python 读 CSV、计算并返回图表 |
| **P1** | gVisor、资源与磁盘配额、Sandbox Broker、生命周期管理 | 死循环、fork 炸弹、写满 workspace/tmp 均不影响宿主机 |
| **P2** | Redis Streams、Postgres Checkpointer、Run 与事件元数据 | `kill -9` Worker 后从 checkpoint 续跑，已完成步骤不重做 |
| **P3** | HITL、取消、多用户隔离、配额限流、工具幂等 | 审批走通；崩溃恢复不重复写；30 并发不崩溃、不串数据 |
| **P4** | ~~OpenTelemetry、MinIO 产物、成本看板~~ | ~~能定位单个 Run 的完整 Trace 与 Token 花费~~ → **三项交付物在 2026-08-12/13 全部撤除**。「定位」与「花费」都改到外部的 Langfuse 上看；平台内留下的是 JSON 日志（排障）与 `runs.tokens_*`（配额闸门的判据）|

**P5 不在这张基线表里** —— 它是 P4 之后为支撑前端补的一层接口，目标由[运行时设计 §5.7](../01design/05runtime-design.md) 的接口契约表给出，不另立验证标准。

验收脚本是 [`script/test/verify.sh`](../../script/test/verify.sh) **一个文件，62 条判据**（P8 补 6 条、P9 补 6 条、P10 补 7 条、P11 补 7 条、P12 补 1 条）。判据编号沿用各期计划 §4 的编号（`P1③`、`P2①`…），每条该看到什么、失败时看哪里仍写在各期计划的 §4 与 §8。

> **2026-08-13 由七个脚本合并而来**（`p1.sh` / `p2.sh` / `p3.sh` / `p4.sh` / `acceptance.sh` / `hostile.sh` / `session.sh`）。原来是逐级转调，`p4.sh` 调 `p3.sh` 调 `p2.sh` 调 `p1.sh` 调另外两个，跑 `p4.sh` 等于把五期跑一遍。**结果表里因此断了六个号**：P1②⑤、P2④⑤、P3⑦、P4⑧ 原本只是「去跑另一个脚本」，不验任何东西 —— 删掉的是转调，它们指向的判据一条不少。合并的另外两个理由（四份各不相同的前置检查、每多一级转调就多一次「未验」的翻译）与一并修掉的六处漂移，写在脚本文件头。
>
> **别与别的东西抢这套栈** —— 它会 `kill -9` broker 与 worker、重启三个进程、停 worker、压限流。同时有人在用的结果是双方都红，且红在互不相干的地方（[P4 §8.10.1](./P4-plan.md) 实测踩过）。

> **每份计划的固定骨架**：§1 边界（做什么／不做什么，不做的每条写明由哪期偿还）→ §2 与上游文档不一致处的本期定案 → §3 环境前提 → §4 验收标准（一条命令能跑完）→ §5 任务分解（每步独立可验，未过不进下一步）→ §6 回归关系 → §7 待决事项。
>
> **§1.3 的「明确不做」是这套文档最该认真读的一节** —— 它是技术债的登记簿。每条都带偿还期次，翻一遍就知道现在的平台差在哪，而不必去猜。
