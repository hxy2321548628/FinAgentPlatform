"""Prometheus 指标：定义、采集与导出。

**三个进程各暴露自己看得见的那部分**，谁都不去替别人回答：

| 进程 | 它独有的观测位置 |
|---|---|
| api | Postgres 与 Redis 里的平台状态：run 各态计数、队列积压、当日 token |
| broker | 唯一持有 `docker.sock` 的进程，因此只有它数得出沙箱容器与它们吃掉的内存 |
| worker | 唯一调用模型的进程，因此只有它量得到一次 LLM 调用有多慢、失不失败 |

**绝大多数指标是「现查」的 gauge，不是进程里攒着的计数器**，因此每次抓取现建一个
注册表填完就扔。这样既没有模块级可变全局，也不会留下「某个用户昨天有量、今天没量，
于是那条时间线永远停在昨天的值」这类过期序列。只有 LLM 那两个真正累积的指标是长生命周期的
（`llm.LlmMetric`）—— 直方图与计数器的语义本就要求跨请求存活。
"""
