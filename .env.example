# 复制为 .env 后填入真实值。.env 已被 .gitignore 忽略，不要提交。
#
# **这份文件有两类读者，规则不一样**：
#
#   1. 平台自己的三个进程 —— 走 pydantic_settings，按绝对路径读仓库根这一份，写在这里就生效。
#   2. `docker compose` 的变量插值（`${...}`）—— **它读的是 compose.yml 同目录的 .env，
#      也就是 deploy/.env**，跟这一份天然不是同一个文件。靠 `ln -sf ../.env deploy/.env`
#      接上（新克隆的仓库要建一次，链接本身不入库）。
#
# 第 2 条不接上的话，任何一条 compose 子命令都在**解析阶段**就失败 —— 连 `stop` 与 `ps`
# 都算，那时容器还没被碰到一下。
#
# 走 compose 插值的四项：HTTP_PORT、SANDBOX_USER、SANDBOX_WORKSPACE_ROOT、
# SANDBOX_QUOTA_DEVICE，各自记在下面所属的分组里。**只有最后一项仍须 export** ——
# 它是 loop 设备号，每次挂载都可能变，写进文件必然过期：
#
#   export SANDBOX_QUOTA_DEVICE="$(findmnt -no SOURCE --target "$(pwd)/data/sandbox")"
#
# **接上之后这份文件里的值就真的生效了**，此前被 export 盖住的错值会立刻现形 ——
# 接之前先核对一遍 SANDBOX_WORKSPACE_ROOT 是不是这台机器上真实的那个路径

# ---- 部署 ----
# nginx 对外监听的端口（**compose 插值**）。留空即 80。
# 开发机上 80 常被占，或想同时起两套栈时才需要动它
HTTP_PORT=80

# ---- LLM（ADR-0009）----
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 模型 ID 以 GET ${DEEPSEEK_BASE_URL}/models 返回为准
MODEL_MAIN=deepseek-v4-pro
MODEL_AUX=deepseek-v4-flash

# ---- MCP 凭据（P6 决策 F4）----
# 键是 mcp_servers.credential_key，值是**整个 Authorization 头的内容**。
# 不设凭据的服务不必在这里出现；改了这里要重启 worker 才生效。
#
# **凭据不进库、不进日志、不进事件流**：mcp_servers 那张表要被前端读（目录卡片），
# 于是「谁能读库」与「谁能读凭据」必须是两件事。
# **所有教师共用一把 key，用量分不开** —— 外部服务按调用计费时，
# 「哪个课题组吃光了额度」平台答不上来，这是主动接受的代价。
MCP_CREDENTIALS={"market-data":"Bearer xxx","report-search":"token yyy"}

# ---- 认证（ADR-0010 / ADR-0011）----
# 首个管理员。**只在 users 表为空时用一次** —— 否则运维改过口令之后，
# 一次重启就把它改回这里写的那个，而这种回退不报错。
# 代价明确接受：凭据会出现在进程环境里，能登上服务器的人 docker inspect 就看得到
ADMIN_NAME=admin
ADMIN_PASSWORD=change-me-before-deploy
# 登录态多久不用就失效，秒。每次请求都会把它推回去，因此是滑动过期。
# **Redis 重启 = 全员重新登录**：session 存在 Redis 里，这不是故障，是选它的代价
SESSION_TTL_SECOND=604800

# ---- 配额与限流（架构 §6.4 §7.5）----
# **下面这几个数全是从一个样本外推出来的初值**，外推方式写在 app/quota/policy.py
# 的常量旁边。要校准就先攒真实用量分布，别凭感觉改这里。
# 留空即用代码里的默认值，这三行只是把可调的位置摆出来。
#
# token 日配额，**按未命中部分计，cache_read 不算**（按 input 总数扣会高估约 1.6 倍，
# 且方向性地惩罚长会话）。教师与学生权限完全相同，配额是二者唯一的实质差别。
#
# **`null` 是「不限」，`0` 是「一次都不许跑」** —— 默认只有 admin 不限：它是平台的
# 运维出口，被自己的闸门挡住时连「查清楚为什么」也一起做不了了。别顺手把 reviewer
# 也划过去，它跑分析时就是个普通老师
# QUOTA_TOKEN_DAILY={"admin":null,"reviewer":1000000,"teacher":1000000,"student":400000}
# 同时在跑的 run 上限 = 沙箱池容量 / 预期同时在线人数。必须明显小于 SANDBOX_MAX_CONTAINER。
# **这一项对 admin 照常生效**：它拦的是「一个人占满整个沙箱池」，那是共享资源
# QUOTA_CONCURRENT_RUN={"admin":3,"reviewer":3,"teacher":3,"student":2}
# 配额按哪个时区的零点重置。**统计窗口与「几点重置」的提示语共用它** ——
# 容器里 TZ 是 UTC，若让提示语自己去转本地时区，印出来的「00:00」实际是早上八点
# QUOTA_RESET_TIMEZONE=Asia/Shanghai
# output token 折算成配额当量的权重。**目前 1:1** —— 手里没有可核对的价目表，
# 而拍一个比 1 更「精确」的数字，只会让后来的人以为它是算出来的
# QUOTA_OUTPUT_WEIGHT=1
# 接口频率：每窗口多少次。**宁松勿紧** —— 这道闸误伤的是正常用户
# RATE_LIMIT=120
# RATE_LIMIT_WINDOW_SECOND=60
# 单个上传文件的字节上限。**改了要同步改 deploy/nginx.conf 的
# client_max_body_size**，两边不一致时大的那一侧形同虚设；直接跑 uvicorn 时
# 没有 nginx，挡它的只有这一项
# UPLOAD_MAX_BYTE=67108864

# ---- Postgres（ADR-0008，会话 checkpoint 与 run 元数据）----
# 同一组值两边用：官方镜像据此建库，api 据此拼 DSN。
# **连接串是拼出来的而不是配一整条**，因为 compose 部署与本机跑 uvicorn 只差主机名一项 ——
# compose 里只覆盖 POSTGRES_HOST，口令因此不必经过 compose 的变量插值
POSTGRES_HOST=127.0.0.1
# 本机直接跑 uvicorn / 跑门禁时连的端口，须与 compose 里映射出来的一致（那边写死 5433，
# 为的是躲开系统自带的 5432）。走 compose 时 api 用的是容器网络里的 5432，由 compose 覆盖
POSTGRES_PORT=5433
POSTGRES_USER=zuel
POSTGRES_PASSWORD=zuel
POSTGRES_DB=zuel
# 数据目录。**必须是宿主机路径，不能用命名卷** —— `docker compose down -v` 会连卷一起删，
# 而 checkpoint 一丢，中断的任务就再也接不上。相对路径以 deploy/ 为基准
POSTGRES_DATA_DIR=../data/postgres

# ---- Redis（ADR-0005 / ADR-0006，事件通道与任务队列）----
# 无口令，因此配一整条即可。compose 里覆盖成 redis://redis:6379/0
REDIS_URL=redis://127.0.0.1:6379/0
REDIS_DATA_DIR=../data/redis

# 工作目录里的文件下载走不走 nginx 直发（X-Accel-Redirect）。**只在 nginx 后面才有效** ——
# 开发机直接跑 uvicorn 时置 true 的话，浏览器只会收到一个空响应，而 HTTP 状态码是 200，
# 看不出哪里不对。**还要求 nginx 挂到了同一个 workspace 根**。compose 里已置 true 并挂好了卷
FILE_DIRECT_SEND=false

# ---- 沙箱 ----
# 镜像须先构建：docker build -f deploy/sandbox.Dockerfile -t zuel-sandbox:latest .
SANDBOX_IMAGE=zuel-sandbox:latest
# 各会话 workspace 的宿主机根目录。默认在仓库内，生产改到独立数据盘。
# **compose 插值**：那边 bind mount 与 broker 的环境变量都取插值结果。
# 这个默认值只够开发机开箱即跑，**挂了 XFS prjquota 的机器上必须改成挂载点本身**，
# 否则 workspace 落在一个没有配额的目录里 —— 而 broker fail-closed，会拒绝建会话
SANDBOX_WORKSPACE_ROOT=/data/sandbox
# 容器数上限按「可用内存 / 单沙箱 2GB」推算，32 核 64GB 的机器取 20
SANDBOX_MAX_CONTAINER=20
SANDBOX_IDLE_TIMEOUT=1800
SANDBOX_QUEUE_TIMEOUT=600
# 租约多久没人碰就强制归还。api 崩在申请与归还之间时的兜底 ——
# 没有它，一次崩溃就永久少一个沙箱名额。要远大于一个 run 里两次工具调用的间隔
SANDBOX_LEASE_TIMEOUT=1800

# ---- 沙箱加固（架构 §7.3.2）----
# 只列有中间档的几项。只读 rootfs、cap-drop=ALL、no-new-privileges 不可配 ——
# 它们只有开和关，做成开关就是给静默失守留入口。
# runsc 即 gVisor，须先跑 deploy/setup-gvisor.sh；换成 runc 等于退回一层容器边界
SANDBOX_RUNTIME=runsc
# 零出网。P1 定案不部署 devpi，因此 agent 装不了任何包，缺库只能重建镜像
SANDBOX_NETWORK=none
SANDBOX_MEMORY=2g
SANDBOX_CPUS=1
SANDBOX_PIDS_LIMIT=128
# /tmp 吃的是宿主机内存而非磁盘，计入单沙箱 2g 预算之内
SANDBOX_TMP_SIZE=512m

# ---- 磁盘配额（ADR-0015）----
# 每会话 workspace 的硬上限。要求承载目录的文件系统是 XFS 且以 prjquota 挂载，
# 开发机用 deploy/setup-xfs.sh 造。留空则不设配额，只有 CI 与没挂 XFS 的机器才该留空
SANDBOX_DISK_QUOTA=5g
# xfs_quota 要 CAP_SYS_ADMIN，而两种跑法里调它的人不是同一个：
#
#   走 compose      —— broker 容器内是 root，不该加 sudo。**这一行盖不住它**：
#                      compose.yml 的 environment: 显式写死 xfs_quota，
#                      而 environment: 的优先级高于 env_file（2026-08-14 用
#                      `docker compose config` 核实过，broker 解析出来就是 xfs_quota）。
#   直接跑 uvicorn  —— 是当前用户在调，权限不够。setup-xfs.sh 已放行免密 sudo。
#
# **因此默认值给 "sudo xfs_quota"**：走 compose 时它被覆盖掉，不起作用；
# 直接跑 uvicorn 时它正是对的那个。反过来填 xfs_quota 的话，唯一起作用的那个
# 场景恰好是错的 —— 而**权限不够时 xfs_quota 只往 stderr 打一句然后退出 0**，
# 配额一个都设不上而无人知晓，沙箱可以把整块盘写满。
#
# 与「broker 拿到 /dev/null 当配额设备」是同一个坑的两种形态：都静默失效。
# api / worker 也会从这里读到值，但它们从不调 xfs_quota（只有 broker 构造 XfsQuota），
# 那个值是惰性的
SANDBOX_QUOTA_COMMAND="sudo xfs_quota"

# ---- Agent Worker（ADR-0005）----
# 一个 worker 同时驱动几个 run。**副本数解决的是可用性，这一项才是吞吐** ——
# 一次分析绝大部分时间在等模型和等沙箱
WORKER_CONCURRENCY=8
# worker 多久给自己手上的任务消息续一次命，秒
WORKER_HEARTBEAT_SECOND=15
# 任务消息闲置多久后允许别的 worker 认领，毫秒。崩溃恢复的延迟上限就是它。
# 要明显大于续命间隔，否则健康的 worker 会被别人当成已经死了
WORKER_CLAIM_IDLE_MILLISECOND=60000

# ---- Langfuse（2026-08-13）----
# **它是外部服务，不由本项目的 compose 编排** —— 自己 clone 一份单独起。
# 三项任缺其一即整个关掉：宁可没有追踪，也不要「配了一半、以为在记其实没记」。
#
# **compose 部署时不能填 127.0.0.1 或 localhost** —— 那是容器自己的回环。
# 填 host.docker.internal（compose.yml 已给 worker 与 api 都配好 host-gateway），
# 或者内网里那台机器的实际地址。直接跑 uvicorn 时才填 127.0.0.1。
#
# **api 也读它**（P11）：用量看板与设置页的数字要向 Langfuse 查。填错的症状是
# 那两处显示「账本未接入」，而 worker 那侧的上报同时也在静默失败 —— 两件事
# 一个原因，但看起来像两个毛病。
#
# **这条路上会送出会话全文**：教师的提问、上传数据的内容、模型的答复都在 trace 里。
# 这是 2026-08-13 主动接受的边界变更，见数据设计 §6.3
LANGFUSE_BASE_URL=
# 在 Langfuse 界面里建项目后拿到的一对密钥。**secret 是凭据，不入库**
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# 模型单价，**每 1 个 token 的 USD**（不是每百万）。每百万 input 收 $0.27 就写 0.00000027。
# 只被 deploy/register-model-price.sh 读，业务代码不认识它们 —— 平台侧的配额闸门
# 数的是 token 不是钱。
#
# **不配就不注册，不要填占位数字**：Langfuse 内置的 100 个模型价格里一个 deepseek
# 都没有，缺价格时 total_cost 恒为 0；而一个编出来的单价会算出看着像模像样的费用，
# 那比 0 更难发现错
MODEL_MAIN_INPUT_PRICE=
MODEL_MAIN_OUTPUT_PRICE=
MODEL_AUX_INPUT_PRICE=
MODEL_AUX_OUTPUT_PRICE=

# ---- sandbox-broker（ADR-0004）----
# 唯一持有 docker.sock 的进程。只在内网监听，不经 Nginx 暴露
BROKER_URL=http://127.0.0.1:8100
# 沙箱以谁的身份跑。留空则用当前进程的 uid:gid（直接跑 uvicorn 时正确）；
# **compose 部署时必须显式设成宿主机上 workspace 的属主**，因为 broker 容器内是 root。
# **compose 插值，且是唯一没有默认值的一项** —— 缺了它连 `stop` 都跑不了。
# 刻意不给默认值：不给的话 broker 建出来的 workspace 目录沙箱写不进去，
# 而症状不指向权限（execute 全部成功，只是产物一个都没有）。响亮的失败胜过静默的错
SANDBOX_USER=1000:1000
# Broker 持有的 Skill 版本仓库；compose 内外必须是同一个绝对路径。
SKILL_ROOT=/data/skill
# 承载 workspace 的块设备。**compose 部署且要磁盘配额时必填**：broker 在容器里
# 调 xfs_quota，而它要打开这个设备 —— 没有设备节点时它的每条命令都只往 stderr
# 打一句然后退出 0，配额一个都设不上。用 `findmnt -no SOURCE --target <workspace 根>`
# 查（loop 挂载每次挂载后可能变号），setup-xfs.sh 结尾会直接报给你。
# 这个变量由 compose 插值使用，因此要 export 到 shell，不是只写在这里
SANDBOX_QUOTA_DEVICE=/dev/loop0
