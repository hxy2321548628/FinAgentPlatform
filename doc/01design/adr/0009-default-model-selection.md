# ADR-0009：默认模型 deepseek-v4-pro + 辅助模型下沉

| 项 | 值 |
|---|---|
| 状态 | **已接受**（2026-07-31：出网通路与合规结论均已确认，见主文档 §10.1、§2.4） |
| 日期 | 2026-07-29 |
| 决策人 | hxy |
| 主文档关联 | §4.3 技术栈全景、§10.1 阻塞项 |

## 背景

平台需要选择驱动智能体的 LLM。金融分析是典型的多步推理任务：读数据 → 判断该用什么方法 → 写代码 → 看结果 → 修正 → 得出结论。

## 决策

- **主模型**：`deepseek-v4-pro`，开启 thinking，`effort` 先设 `high`，再按实际路由压测调整
- **辅助调用**（意图分类等轻量任务）：下沉到 `deepseek-v4-flash` 以控制成本
- **模型接入层做成可替换**：若后续因保密要求转为内网私有化部署，只替换该层，不影响上层设计

## 理由

金融分析这类**多步推理**任务，提高 effort 的收益比换用更大的模型更明显 —— 多步任务中每一步的判断质量会沿链路累积，推理深度的边际收益高。

辅助调用（意图分类、标题生成这类）不需要推理深度，用主模型是纯浪费，下沉到轻量模型可显著降低成本。这与 §3.1 中「成本可控」被列为 P1 质量属性一致。

**接入层可替换**这一条原本是为对冲出网与合规风险而设的。2026-07-31 两者均已确认放行（主文档 §10.1），该风险不再存在，但**这一条仍然保留** —— 理由已从「对冲阻塞」变为「模型迭代换代频繁，接入层解耦的成本很低而收益长期」。

## 被放弃的备选

> **TODO**：本节尚未实际展开。当前的选择是基于经验判断而非横向评测。
> 至少应对比：通义千问、GLM、Kimi 等国内可用模型，以及内网可私有化部署的开源模型（Qwen / DeepSeek 开源权重）。
> 对比维度应包括：多步工具调用的可靠性、代码生成质量、中文金融语料表现、成本、以及**是否支持私有化部署**（这一条因 §2.4 未决而权重很高）。

## 后果

**正面**：
- 推理深度匹配任务特性
- 辅助调用下沉可观地降低成本
- 接入层抽象使模型切换不波及上层

**代价**：
- **依赖公有云 API**。出网通路已确认可用（§10.1），但仍是一条外部依赖：服务商故障或网络策略变更会直接导致平台不可用
- **教师数据会作为 prompt 的一部分离开内网**。§2.4 已确认合规允许且数据不含敏感内容，因此无需脱敏（§7.4）；但若将来平台开始承载敏感数据，本决策需重新评估
- 受 LLM 服务商的 rate limit 约束，并发高峰时可能集中报错，worker 侧需要退避重试与降级策略（尚未设计，见 §10.2 风险登记）
- 成本随使用量线性增长，必须配合 §6.4 的配额机制

## 待确认事项

| 项 | 说明 |
|---|---|
| ~~模型名称核对~~ | **已核对**（2026-08-02，P0 探针）。`GET /models` 实际返回 `deepseek-v4-pro` 与 **`deepseek-v4-flash`** —— 原文档写的 `deepseek-v4-flush` 确为笔误，已全文改正 |
| ~~出网通路~~ | **已确认可用**（2026-07-31，§10.1）。部署时用 `curl` 在目标机复验一次 |
| ~~数据合规~~ | **已确认放行**（2026-07-31，§2.4） |
| **effort 取值** | `high` 是起点，需按实际压测调整。这直接影响成本与延迟 |
| **横向评测** | 见上方「被放弃的备选」中的 TODO |

## 重新评估的触发条件

- 出网策略或合规要求**后续发生变化**（原为「任一不通过」，两者已于 2026-07-31 确认通过）→ 必须重新决策，转内网私有化部署
- P0 实测发现多步工具调用可靠性不足
- 成本超出学院预算
