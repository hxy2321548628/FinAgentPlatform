# P13 实施计划：先有尺子，再改缓存

| 项 | 值 |
|---|---|
| 文档状态 | 步骤一–十一、十三已完成；**步骤十二（第二轮对比跑）未跑 —— 本期主判据 P13⑤／⑥ 因此仍是「未验」** |
| 当前版本 | v0.3 |
| 作者 | hxy |
| 日期 | 2026-08-19 |
| 上游文档 | [P13-decision.md](./P13-decision.md)（**v0.6，24 条定案**，本期落地其中的 C 组全部 + A1 + A2 + A8 + A9 + A7 的一半） |
| 下游文档 | P14-plan.md · P15-plan.md（均未撰写） |

### 版本历史

| 版本 | 日期 | 修改人 | 说明 |
|---|---|---|---|
| v0.3 | 2026-08-19 | Claude | **加开步骤十三，起因是步骤十的「✅ 完成」是假的。** 卸载的阈值配下去了，机制却一次都没触发过 —— 真因不是阈值而是**落点**：框架算出的 `/large_tool_results` 过不了平台的路径闸，而写盘被驳回是静默的。**同一个 bug 打中压缩的归档**（26 次压缩、26 次 `file_path` 为空）。步骤十那条实施记录里「E03 单次工具输出约 13856 token」的估算一并作废 —— 直接量事件流是**最大 1820 字符**，那个分子里绝大部分是历史累积 |
| v0.2 | 2026-08-19 | Claude | **加开第二轮（步骤十–十二），起因是读《深入理解 AI Agent》第 2 章之后发现步骤六、七的结论判错了一处、漏了一处。** ① **主判据由 `cache_hit_rate` 改成 `cost_yuan`**（[decision v0.5 的 A2](./P13-decision.md)）—— 步骤七据命中率判定「尾部注入有害」，按真实单价复算方向是反的；§4.2 的 P13⑤ 随之改写。② **§7 第 1 条的处置作废**：「按需注入」建立在那个错判上，按书 2.6.4 改为**持久追加**。③ **新增步骤十（A9 工具结果预算）**，它排在最前，因为卸载之后 A1／A8 的基线都要重测 |
| v0.1 | 2026-08-19 | Claude | 初稿。**开工前六处实测已跑完**（§2），其中两处推翻了决策文档的预设：DeepSeek 吃不到任何 prompt-caching 中间件、压缩阈值可按中间件名替换来钉死。**评估集与数据已先行落地**（§2.6），那是写本计划时为验证判据可行性做的 |

> **本期的一句话验收标准**（[D1](./P13-decision.md)）：**同一批题，改造前后每题成本与压缩触发次数可比，且噪声带已经量出来。**
>
> ⚠️ **v0.2 把「cache 命中率」换成了「每题成本」。** 命中率是个比值，分子分母同向变化时它不敏感 —— 步骤七正是栽在这里：命中率一致下降 5–6 个点，按单价复算却是 9 题里 7 题成本下降。详见 [decision 的 A2](./P13-decision.md)。
>
> **顺序上有一条不能颠倒的**：尺子必须先于被测物。步骤一到四把尺子做出来并量出基线，步骤五之后才动 agent 本体 —— 先改后量的话，「改造前」那一栏永远是空的。

---

## 1. 边界

### 1.1 本期做什么

| 块 | 来自 | 内容 |
|---|---|---|
| **尺子的前置** | A7 的一半 | 撞递归上限要能与其它内部错误分开（现在混在 `INTERNAL` 里）；压缩触发次数要有采集通路（现在取不到） |
| **尺子本体** | C 组全部 | 评估集 10 题 + runner + 13 项指标 + 本地落盘 + Langfuse experiment 上报 |
| **上下文上限** | A1 | 探出 `deepseek-v4-pro` 的真实上限，把压缩阈值从模型 profile 推导改成平台自己的显式常量 |
| **缓存友好** | A2 | 审计前缀里有没有每轮都变的内容，并把规则写进代码而不只是文档 |
| **尾部注入** | A8 + A7 的另一半 | 一个中间件、一个块、内部分节；本期只填**环境状态**那几节（剩余步数、剩余磁盘、已耗 token、`outputs/` 现有什么、本会话装了哪些包） |

### 1.2 明确不做

| 不做 | 理由 | 由谁偿还 |
|---|---|---|
| 提示词优化 | 尺子刚做出来、agent 形状还要变（P14 加两个工具），对着还要变形的东西调提示词是白调 | P15（A3） |
| 记忆系统 | 同上，且记忆的隔离口径还没回答 | P15（A4） |
| 用户信息与 reminder 的**内容** | 本期只定注入的位置与预算，填什么是 P15 的事 | P15（A5、A6） |
| 状态栏的**任务进度**一节 | 数据源是 `todo_write`，那个工具本期不存在 | P14（B1） |
| `ask_user_question` / `todo_write` | 两个工具都要动事件模型与前端 | P14（B1、B2） |
| pairwise 判分 | 本期没有可比的基线版本，P13 只负责把输出落盘 | P15（C7） |
| 评估集进 CI | 每跑一次都是真实 LLM 费用，它是按需跑的工具不是门禁 | 不重估（[§7](./P13-decision.md)） |
| 中文乱码的自动判定 | 已定案走人工抽检 | 不重估（C3） |

---

## 2. 开工前实测与本期定案

**六处已跑完，两处推翻了决策文档的预设。**

### 2.1 Langfuse 的端点边界 —— 测了两轮，第一轮是错的

[P13-decision C6](./P13-decision.md) 记着完整对照表。**这里只记与本计划有关的三条**：

- 数据集、prompt、score-config、annotation-queue **可用** —— 评估集有地方放、rubric 有地方托管、人工抽检有正式载体；
- 分数与实验结果 **读得回来**（`/api/public/v3/scores`、`/experiments`、`/experiment-items?fields=io`），因此本地 JSON 是便利副本而非唯一通路；
- trace 与 session 的读取在 v4 **真的取消了**，没有替代 —— 内容挂在 observation 上（`/v2/observations`）。

> ⚠️ **第一轮结论「写得进读不回」是错的**，错在只测了 v3 遗留端点、且把 404 响应体截断了（`_deprecation.replacement` 字段正好被切掉）。**教训写进本计划的 §3 环境前提**：打到 4xx 先完整打印响应体。

### 2.2 DeepSeek 吃不到任何 prompt-caching 中间件 —— **推翻 A2 的预设**

deepagents 的 `append_prompt_caching_middleware()` 只装三家：`AnthropicPromptCachingMiddleware`、Bedrock、Fireworks，且都带 `unsupported_model_behavior="ignore"`。**DeepSeek 一个都不沾，装了也是空转。**

这不是坏消息，因为 DeepSeek 的 prompt cache 是**服务端按前缀自动命中**的（平台实测 62% 与 31% 就是证据，见 [P13-decision §2](./P13-decision.md) 第 4 条）。

> **A2 的形状因此变了**：不是「装一个缓存中间件」，而是 **「审计前缀里有没有每轮都变的内容，并把这条规则钉进代码」**。能做的事只有一件 —— 让前缀稳定。步骤五因此与 A8 合并：新增的动态内容一律走尾部注入点。

### 2.3 压缩阈值可以钉死，路径是「按中间件名替换」

`create_summarization_middleware()` 这个公开构造器**不暴露阈值**（只有 `summary_prompt` / `trim_tokens_to_summarize` / `token_counter`），阈值来自 `compute_summarization_defaults(model)` 从模型 profile 推出来的 `('fraction', 0.85)` × `max_input_tokens=1000000`。

但实现类 `_DeepAgentsSummarizationMiddleware.__init__` **接受 `trigger` 参数**，且它的 `.name` 报的是公开别名 `"SummarizationMiddleware"`；而 `_apply_custom_middleware()` 的规则是 **「custom 里 `.name` 与基础栈里某个重名就原地替换，保持栈序」**。

> **因此 A1 的落地路径是**：平台自己构造一份 summarization 中间件、显式传 `trigger=("tokens", 平台常量)`，作为 custom middleware 传进 `create_deep_agent` —— 它会替换掉基础栈里那个，而不是叠加一份（叠加正是 [A1](./P13-decision.md) 否掉的选项 a）。**替换是否真的生效，是步骤三的验收判据，不是假设。**

### 2.4 撞递归上限现在混在 `INTERNAL` 里 —— 位置已定位

[`executor.py:274`](../../src/app/run/executor.py#L274) 是一句 `except Exception as exc:` 直接落 `RunErrorCode.INTERNAL`。`GraphRecursionError` 走的就是这一支，全仓 `grep recursion` 没有任何一处认它。

**改动面很小**（加一个 `except GraphRecursionError` 分支 + 一个枚举值 + 前端文案），但它是**尺子的一部分**：`error_code` 是 13 项指标之一，撞上限分不出来，失败归因那一栏就是坏的。

### 2.5 `compaction_count` 没有采集通路 —— 本期要补

deepagents 把压缩事件放在私有 state 字段 `_summarization_event`，并把每次压缩追加进一个文件。**平台的事件流里没有任何一种事件对应它**（`EventType` 枚举里没有）。

A1 的主指标就是这个数。**不补通路，A1 改完无法验证是否生效。**

### 2.6 评估集与数据已先行落地

写本计划时为验证「判据写得出来吗」，先造了首批 10 题并上传：

| 落地物 | 位置 |
|---|---|
| 10 题与判据 | [`script/eval/dataset.json`](../../script/eval/dataset.json) |
| 幂等上传脚本 | [`script/eval/upload_dataset.py`](../../script/eval/upload_dataset.py) |
| 数据（含注入的异常值、固定种子的日频生成器） | [`script/eval/data/`](../../script/eval/data/) |
| Langfuse 数据集 | `zuel-finance-eval-v1`（10 条 item，已读回验证） |
| 平台客户端（runner 的第一个模块） | [`script/eval/client.py`](../../script/eval/client.py) |

**probe 覆盖 9 种**：基线 ×2、缺失值、异常值、装包、审批闸门、中文图表、压缩、多轮记忆、不存在的字段。ground truth 是算出来的不是估的（例：行业占比 31.98%／19.57%，年化波动率茅台 0.2010、招行 0.0804，锚题 0.10385374331241026）。

> **C1 的选择性偏差缓解清单已经照着做了** —— 10 题里 7 题是「已知会踩坑的」。**但它有没有真的踩到，要等首轮跑完看 `probe_coverage`。**

### 2.7 开工前三处拍板（2026-08-19）

| 项 | 定案 | 接受的代价 |
|---|---|---|
| runner 用哪个账号 | **建一个专用评估账号** | 建号要进 api 容器算口令哈希、插 postgres（与 `verify.sh` 同一套做法），多一个脚本。换来的是评估会话不混进教师自己的历史、配额也不互相占 |
| 首轮接不接 judge | **不接，只跑 code-based** | **判分噪声带因此本期后半段才量得到** —— 见下 |
| 压缩在平台侧怎么表示 | **新增 `EventType.COMPACTION`** | 前端的事件 schema 要同步改，P6 那几条 SSE 判据要重跑。换来的是教师看得见「上下文已压缩」——长分析里这是个真实信号，之后的回答可能丢了前面的细节 |

> ⚠️ **第二条有一个必须写下来的后果。** [决策文档 §10.2](./P13-decision.md) 的第 2 条必测项是「判分方差」，首轮不接 judge 就量不出它。**这条不取消，移到步骤八**（那时 rubric 已经稳定，且手里有两轮真实输出可判）。
>
> **但确定性指标的噪声带本期必须量，而且它比判分噪声更要紧**：P13⑤ 那条主判据比的是 cache 命中率与压缩次数，**没有它们的噪声带，就没法说「这次提升超过了波动」**——而那正是本期唯一要证明的事。噪声带因此拆成两条判据，见 §4.2。

---

## 3. 环境前提

| 前提 | 怎么确认 |
|---|---|
| 六个服务起着 | `make ps`，且宿主机 quota 设备与 broker 映射设备一致 |
| Langfuse 4.10.0 可达 | `curl -s localhost:3000/api/public/health` |
| **两个地址变量都要指到 localhost** | `langfuse-cli` 认 `LANGFUSE_BASE_URL` 优先于 `LANGFUSE_HOST`，而 `docker/.env` 里那个值是 `host.docker.internal`（给容器用的名字），宿主机上解析不了 |
| prompt 取的是 `production` 标签 | `get_prompt(name)` 默认取它；只打了 `latest` 的会 404 |
| v4 读端点几乎都要 `fromStartTime` | 不给是 400 不是空结果；`v2/metrics` 还要 `view` |
| 评估账号 | runner 用普通教师账号登录，不走 verify.sh 那套进容器造号 |
| 大数据题的输入 | `python3 script/eval/data/make_daily_price.py` 先生成（714K，固定种子，不入库） |
| **真实 LLM 费用** | 首轮 10 题 × 3 副本 ≈ 30 次真实分析，含一道 15000 行的大题 |

> **打到 4xx / 5xx 时完整打印响应体。** §2.1 那次判错就是把 404 的响应截断了 —— 替代端点的名字就写在被切掉的那半截里。

---

## 4. 验收标准

**分两组，因为它们的成本差两个数量级。**

### 4.1 进 `verify.sh` 的（要真实模型调用但只跑两三道简单分析，判据总数 62 → **64**）

| 判据 | 该看到什么 | 失败时看哪里 |
|---|---|---|
| **P13①** | 撞递归上限的 run 记成 `RECURSION_LIMIT` 而不是 `INTERNAL`：把上限配成极小值跑一道普通题，`runs.error_code` 与 `run.failed` 事件里都是新码，前端文案可读 | `executor.py` 的异常分支；上限是不是真的读到了配置 |
| **P13②** | 压缩触发得到、也数得出来：把阈值配成极低值跑一道普通题，事件流里出现压缩事件且计数 ≥ 1；恢复正常阈值后同一道题计数为 0 | 采集通路接在哪一层；`_summarization_event` 是否被 checkpoint 短路（P8 踩过同型） |
| ~~**P13③**~~ | **不单立判据，由 P13② 蕴含** —— 默认阈值是 85 万，普通题在 2000 下压了、在默认下不压，这件事本身就证明生效的是平台那份；另有单测断言 `.name` 与 trigger（`context_test.py`） | 同上 |
| ~~**P13④**~~ | **不进 `verify.sh`，由单测覆盖**（`prefix_test.py` / `tail_test.py`）：块的位置、不堆进历史、预算上限、系统提示词逐字节不变。**块长什么样只有模型看得见**，事件流里看不到，真跑判据验不了它 | — |

> **改配置只重建 worker，且必须 `--no-deps`**：worker 的 `depends_on` 里有 broker，不加的话 compose 会连带把 broker 也重建，而验收脚本那个 shell 里没有 `SANDBOX_QUOTA_DEVICE` —— 设备回落成 `/dev/null`、配额 fail-closed，此后每次建会话都 500，**而症状一个字都不指向这里**（2026-08-19 实测踩过）。
>
> **P13① 与 P13② 都要「配成极端值再跑一次」** —— 这是[§9 总原则](./P13-decision.md)的直接落地：判据要能回答「这次真的触发了要测的场景吗」，而不是「这次没报错吗」。**两条都必须能在正常配置下复原为不触发**，否则证明的只是「它一直在报」。

### 4.2 评估集那一组（要真实 LLM 费用，按需跑，不进门禁）

| 判据 | 该看到什么 |
|---|---|
| **P13⑤** | **本期的主判据（v0.2 改）**：同一批 10 题，改造前后的 **`cost_yuan`** 与 `compaction_count` 并排可比，**差值超出 P13⑥ 那条噪声带**才算改进。`cache_hit_rate` 仍然照旧采集，但降为诊断量 —— 用途是回答「省下来的钱是从缓存来的还是从少走几轮来的」 |
| **P13⑥** | **确定性指标的噪声带**（本期必须）：10 题 × 3 副本，同题的 **`cost_yuan`**、`cache_hit_rate`、`tokens_uncached`、`llm_rounds` 极差有数。**没有这条，P13⑤ 就只是两个数字大小的比较** |
| **P13⑦** | **尺子自身的校验**：`probe_coverage` 报得出「这批里几题真的触发了压缩、几题真的撞了审批、几题真的装了包」。**任一 probe 命中数为 0 时，那一栏记「未验」不记「过」** |
| **P13⑧** | **判分噪声带**（步骤八，首轮不接 judge 的欠账）：同题三副本的 `plan_quality` 极差有数。**这个值决定 P15 的验收标准长什么样**（[D5](./P13-decision.md)） |

---

## 5. 任务分解

**顺序不能颠倒：步骤一到四把尺子做出来并量出基线，步骤五之后才动 agent 本体。**

| # | 做什么 | 验 |
|---|---|---|
| **一** | 补 `RunErrorCode.RECURSION_LIMIT`，`executor` 里认 `GraphRecursionError`；顺手把 `RECURSION_LIMIT = 60` 从模块常量改成 Settings 配置项（宪法第二条，且 P13① 要靠它造极端值） | 单测覆盖新分支；P13① |
| **二** | 压缩事件的采集通路：从 `_summarization_event` 取到「这一轮压了没有」，映射成**新的 `EventType.COMPACTION`** 并计数；**前端的事件 schema 同步改**（它照着 `event/model.py` 抄） | 单测 + P13②；前端那份 schema 的校验用例 |
| **三** | runner 收尾：建专用评估账号的脚本、`metric.py`（事件流 → 指标 + 判据评估）、`run_eval.py`（主流程 + experiment 上报 + 本地落盘）。**含一次不调 LLM 的自检**：判据评估器对着造好的假答复能判对、也能判错 | 判据评估器的单测（正反两向）；自检脚本 |
| **四** | **基线跑一轮（不接 judge）**：10 题 × 3 副本，落 `script/eval/result/baseline-*.json` 并上报 experiment。跑完做一次开放编码（按 error-analysis 五步）：读失败样本、聚类、形成失败分类 | P13⑥、P13⑦；**这一步的产出决定要不要重造题** |
| **五** | A1：上下文上限探针（构造递增超长输入，看在哪一档真被拒，注意分辨网关的限制与模型的限制），把探出来的数钉成平台常量，按名替换中间件 | P13③ |
| **六** | A2 + A8 + A7 的一半：审计前缀里每轮变化的内容；建尾部注入中间件（一个块、内部分节、预算上限）；填环境状态那几节（剩余步数／剩余磁盘／已耗 token／`outputs/` 现有什么／本会话装了哪些包） | P13④ |
| **七** | **改造后再跑一轮**，与步骤四的基线并排比 | **P13⑤（主判据）** |
| **八** | 补 judge：在 Langfuse 建 `judge-rubric` prompt 并打 `production` 标签（rubric 按金融分析领域写，**不抄系统提示词的措辞**），对步骤四与步骤七的两批输出补判分 | **P13⑧**；rubric 的版本号记进结果 JSON |
| **九** | 收尾：P13①–④ 写进 `verify.sh`；建 annotation queue（score config 要一次建全，**队列建了不能改也不能删**）；把首轮结论回写 [P13-decision §10.3](./P13-decision.md) 的两个观察项 | 全量回归一轮，确认没打穿 P0–P12 那 62 条 |

### 5.1 第二轮（步骤十–十二，v0.2 加开）

**起因**：读《深入理解 AI Agent》第 2 章之后，步骤六、七的结论**错了一处、漏了一处**。错的是判据（命中率 → 成本），漏的是分层压缩的第一层（A9）。

**顺序同样不能颠倒，理由和第一轮一样但方向相反**：步骤十会改掉历史长度，A8 的账要在卸载之后的基线上重算 —— 先做十一再做十的话，十一的数要重测。

| # | 做什么 | 验 |
|---|---|---|
| **十** | **A9 工具结果预算控制**：`FilesystemMiddleware` 的 `tool_token_limit_before_evict` 由默认 20000 降到 2000–4000（具体值按 E03 实测定），主图与子图两处都要配。**先量后调**：拿首轮结果里每次工具输出的 token 分布定阈值，不凭感觉 | 单测断言两处装配都带上了阈值；**单跑 E03**（首轮 1.30 元、未命中 12.5 万），看卸载触发次数与成本 |
| **十一** | **A8 改持久追加 + A7 补操作策略**，四件事一起：①块写回 state（`response.result = [HumanMessage(block), *response.result]`），②**回退步骤七那个「半程闸门」**（它建立在错判上，且 15 轮才开口而多数题总共 6–11 轮，等于常态沉默），③静态节（已装的包，只增不减）与动态节（步数）拆开，④每节补一句操作策略（书 2.6.5：只给读数与什么都不给差 2–3 个百分点） | 单测：块进了 state、下一轮请求是上一轮的严格扩展、陈旧块不重复渲染；`prefix_test.py` 那三条仍要绿 |
| **十二** | **第二轮对比跑**：10 题 × 3 副本，与步骤四的基线、步骤七的那一轮三方并排。**判据用 `cost_yuan`**，命中率只作归因。**采集通路已先行补齐**（见实施记录）：`cost_yuan` 与 `offload_count` 此前都不在 `RunFacts` 里 —— 主判据换了而尺子没跟着换，跑完也说不清 | **P13⑤（改写后的主判据）**、P13⑥ 重算噪声带；**卸载触发数为 0 时那一栏记「未验」**，与压缩那次同一条规矩 |

| **十三**（v0.3 加开） | **卸载与归档的落点**：把两个中间件的产物落点钉到 `/workspace` 下。**起因是步骤十根本没生效** —— 阈值不是变量，落点才是 | 单测用**真的** `SandboxBackend` 端到端验（反证：拔掉钉子必须转红）；真机跑一次，看卸载次数与压缩的 `file_path` |

> ⚠️ **步骤十二之前要先跑一遍免费的 43 条** —— 与步骤六同理：十一改变每一次模型调用的消息构成，不先回归的话「分数变了」分不清是改进还是打穿。

---

## 6. 回归关系

| 本期改动 | 可能打穿 |
|---|---|
| `executor` 新增错误码分支 | P2（崩溃恢复）、P3（取消与 HITL）的失败路径判据；前端的失败文案（P6 走查） |
| 中间件栈里多一份平台自己的 summarization | **P0①–⑤ 全部**（每一次分析都过这条栈）、P8（skill 中间件同栈）、P9（子图各自装一份） |
| 尾部注入块改变每轮的消息构成 | P4 的 token 判据、P11 的用量归属与账单对账 —— **块本身要烧 token，两边的数都会变** |
| 事件模型新增压缩事件 | 前端的事件 schema 校验（web 那份照着 `event/model.py` 抄的）、P6 的 SSE hook |
| `RECURSION_LIMIT` 由常量改配置 | P9 的递归上限判据（子图那个 9999 是框架硬编码的，不受影响，但判据文案要对得上） |
| **（十）工具结果卸载阈值调低** | **P0①–⑤ 全部**（每次分析都过）、P4 的 token 判据、P11 的用量对账；**尤其是产物类判据** —— 卸载把工具结果换成一句 `saved at {path}`，凡是靠读工具输出判定的判据都要重看 |
| **（十三）卸载与归档落进 workspace** | **教师的文件树里会多出 `.large_tool_results/` 与 `.conversation_history/`** —— [`browse.py`](../../src/app/sandbox/browse.py) 用的是 `rglob("*")`，pathlib 与 shell 不同，它**匹配隐藏文件**，点号并不会让它们消失。另外这些文件占会话的 5g XFS 配额，保留期清理要一并看 |
| **（十一）尾部块写回 state** | **checkpoint 的形状变了** —— 块进了历史，P2 的崩溃恢复与 P3 的取消／HITL 都在同一条 checkpoint 上；块会出现在 `/replay` 的事件流里吗要确认（它是 `HumanMessage`，可能被当成教师说的话渲染到前端） |

> **步骤六是本期最容易打穿别人的一步** —— 它改变每一次模型调用的消息构成。**步骤七的对比跑之前，先跑一遍免费那 43 条**，否则「分数变了」分不清是改进还是打穿。

---

## 7. 待决事项

**开工前那三处已经问掉并定案**（§2.7）。**开工时挂着的两条，跑完之后都有数据回答了**：

1. ~~**尾部注入块的 token 预算上限定多少。**~~ **✅ 已回答，但问题本身问错了。** 真实上下文上限是一百万（步骤五探针），块那点长度在预算上根本不成问题 —— **真正的代价不在长度，在频率**：块每轮都变，下一轮的历史在同一位置换成了别的内容，缓存匹配到那儿就断。三副本实测未命中 token 涨 20%–200%。~~**处置是按需注入**（每节只在有话说时开口）~~，预算上限维持 1200 字符不变。
   > ⚠️ **2026-08-19 二次修订：机制描述成立，处置作废。** 上面那段机制正是书 2.6.4 的「实现一：每轮替换」，书与实测都指向同一件事，这部分不用改。**错的是处置** —— 「按需注入」是在错判据（命中率）下选的，按 `cost_yuan` 复算它反而更贵（E01 +36%、E09 +26%）。**改为书里的「实现二：持久追加」**，见步骤十一。
2. ~~**首轮跑完要不要重造题。**~~ **✅ 不重造**（判断与理由记在步骤四）。§7 原来那两个触发条件——「接近全过」与「probe_coverage 明显低于 1」——**前者成立但不构成理由**（主判据是连续量不是二值，且题目在成本维度有 20 倍分辨度），**后者只差在压缩那一道**，而它没触发的原因是被测系统的阈值，不是题造软了。

**跑完之后新挂上两条**，都要等下一轮数据：

1. **`CONTEXT_TRIGGER_TOKEN = 40_000` 定高了。** 三副本 30 次分析，压缩触发 **0 次** —— 这批题的历史峰值够不着四万。**（v0.3 补）这一条要连着步骤十三一起看**：那三批数据是在卸载从未生效的前提下采的，历史里塞满了本该挪走的大工具结果 —— 也就是说，**当时的历史长度是被高估的那一版，而它仍然够不着四万**。修完之后历史只会更短，这一条只会更成立。**它是个观察项**：要么把阈值再压低（代价是压缩本身要花一次摘要调用、还会丢细节），要么承认「这类分析根本用不上压缩」并把 A1 的价值重新估一遍。**在拿到数据之前不动它** —— 现在改就是凭感觉调参。
2. ~~**E01 的成本没完全回到基线。**~~ **✅ 已作废（2026-08-19）** —— 这一条本身就是拿命中率当成本的产物。「差 5 个百分点」说的是命中率（0.898 vs 0.947）；按 `cost_yuan` 复算，E01 的三档是 0.0820（基线）→ 0.0718（全量注入）→ 0.0978（按需注入），**按需注入才是最贵的那一档**。这一条不再作为观察项，由步骤十二重新量。

**第二轮新挂上一条**：

3. **持久追加的陈旧状态会累积多少。** 书 2.6.4 明说实现二的代价是「陈旧的状态会在上下文中累积 —— 既占用 token，也要求模型自己关注最新一条而忽略已过时的旧状态」。按 6–11 轮、单块几十字估算是几百 token，**但「模型认不认得最新一条」量不出来，只能看行为**：步骤十二要专门看有没有出现「照着旧步数做决定」的样本（例如块里已经说到第 9 轮，模型仍按第 3 轮的余量安排工作）。**这一条是接受代价，不是无代价** —— 如果累积明显影响判断，退路是给块加轮次序号让模型能自己排序，而不是退回实现一。

**第三轮新挂上一条**：

4. **`.large_tool_results/` 与 `.conversation_history/` 要不要在教师那侧藏起来。** 点号只是命名约定，[`browse.py`](../../src/app/sandbox/browse.py) 的 `rglob("*")` 照样列得出来（pathlib 与 shell 不同）。**先不动**：这两个目录里的东西 agent 要能 `read_file` 读回去，教师看得见也不算错，而「藏」这件事一旦做了就要同时想清楚下载接口、产物判定与保留期清理三处的口径。**触发条件是教师真的反馈了「多出两个看不懂的目录」** —— 在那之前它只是个观察项。另外这些文件占会话的 5g XFS 配额，长会话要留意。

---

---

## 8. 实施记录

> 每步做完在此追加：改了什么、判据是过还是未验、有没有打穿别的东西。
> **没触发到要测的场景时记「未验」，不记通过** —— 这条比判据本身更要紧。

| 步骤 | 日期 | 结果 |
|---|---|---|
| **十三** 卸载与归档的落点 | 2026-08-19 | ✅ 完成，**真机验过**。**步骤十的「完成」是假的**：卸载阈值从 20000 降到 4000、再降到下限 100，触发次数一直是 0。真因不在阈值在**落点** —— `FilesystemMiddleware` 算路径的办法是「后端是 `CompositeBackend` 就取它的 `artifacts_root`，否则按 `/` 算」，平台的后端实现的是 `SandboxBackendProtocol`，落点因此成了 `/large_tool_results`，而平台要求一切在 `/workspace` 下，写盘每次被驳回。**驳回是静默的**：`write` 返回带 error 的结果 → `_offload_tool_message_content` 返回 `None` → 调用方原样留下消息，不抛不记。**同一个 bug 打中压缩的归档**（`SummarizationMiddleware` 同一段逻辑）：历史照常折成摘要，归档写盘全失败，库里 **26 次压缩、26 次 `file_path` 为空** —— 那句「完整历史已存到 …」根本没拼进摘要，折掉的原文没有留底。**修法**：`pin_artifact_path()` 把两个中间件的落点钉到 `/workspace/.large_tool_results` 与 `/workspace/.conversation_history`。**没走 `CompositeBackend` 包装那条路** —— 包装会在每次文件操作与 `execute` 前加一层委托，而 [`remote.py`](../../src/app/sandbox/remote.py) 的模块 docstring 专门强调过异步方法必须全覆写、`delete` 能力探测认的是同步方法，那层风险远大于问题体量。**钉不存在的属性名当场抛** —— `setattr` 到一个改了名的属性不报错，只会多挂一个没人读的字段而框架默认落点照旧生效，症状与修复前一模一样、连线索都没有。**判据归单测不进 `verify.sh`**：用**真的** `SandboxBackend`（要验的正是它那道路径闸），端到端验超限结果真被换成一句路径、文件真躺在盘上；反证跑过 —— 把钉子拔掉同一条立刻转红，`卸载了吗 = False`、盘上零文件。**真机复验**（两个阈值都调到极端值，跑完已恢复并确认容器读到 4000／40000）：卸载 **0 → 3 次**、压缩留底 **0/26 → 12/12**，盘上 `.large_tool_results/` 三个文件、`.conversation_history/{thread}.md` 13783 字节，`outputs/` 的图照常出，run 终态 succeeded。留档会话 `310be86df74f4337bdceb7345faed1d0`、run `d316b22ceb4944b489b75f4106131a05`。**两端门禁全绿** |
| **十二前置** 尺子跟上主判据 | 2026-08-19 | ✅ 完成。**判据换了，尺子没换 —— 这一条差点漏掉**：decision v0.5 把主判据改成 `cost_yuan`，而 `RunFacts` 里根本没有这个字段，`offload_count`（A9 到底拦住几次）同样没有采集通路。**不补的话步骤十二跑完也说不清**，与 `compaction_count` 当初那个缺口是同一种。补法：`cost_of()` 按 `docker/.env` 的三项单价算钱（默认值与之同源，改一边要改另一边）；卸载次数从 `tool_result` 事件的正文里数 `TOO_LARGE_TOOL_MSG` 的痕迹 —— **不需要新的事件类型**，替代文本本来就在事件流里。run 级判据 `mean_cache_hit_rate` → `mean_cost_yuan`、`cache_hit_rate_spread` → `cost_spread`，且 **spread 必须取相对值**：各题绝对成本差二十倍（0.06 元到 1.30 元），按绝对值排，便宜题翻一倍的剧烈波动永远排不上号。新增 `run_eval_test.py` 三条 + `metric_test.py` 四条，`script/eval` 27 条全绿。**顺带修一处文档漂移**：`metric_test.py` 里记的跑法少了 `PYTHONPATH=script/eval`，照着敲必然 ModuleNotFoundError |
| **十一** A8 持久追加 + A7 策略 | 2026-08-19 | ✅ 完成。**块改为写回 state**（`response.result = [HumanMessage(block), *response.result]`，排在模型回复之前 —— 写反了下一轮读起来就是「先答后问」）。**新判据 `test_each_turn_extends_the_previous_call_verbatim`**：逐次比对，第 N+1 次调用的消息序列必须是第 N 次的严格扩展 —— 这条就是「前缀不断」的定义，比数块的个数更贴近要保的那件事。**半程闸门已回退**（它按上限的一半算，60 步要跑到 15 轮才开口，而多数分析总共 6–11 轮，等于从不开口）。**「静态/动态拆开」改了形态**：计划里写的是把「已装的包」与「步数」拆成两块，实做发现真正该拆的不是这两节，而是每节内部的**读数（每轮变）与策略（不变）** —— 策略只在第一次说，之后靠持久追加留在轨迹里，模型仍看得到。物理拆两块反而要引入分组概念，收益还小（静态那节只有几个字）。**计划外必须一起改的一处**：块写回 state 之后会跟着模型回复出现在 `updates` 的 model 节点里，`_map_tool_call` 把它当成「意外形状」每轮告警一次 —— **豁免收窄到只认 `BLOCK_HEADER` 开头的 HumanMessage**，其余非 AIMessage 仍要喊（与压缩事件那次是同一种收窄，一刀切等于把这个信号自己弄哑）。**顺带修了两条过期判据**：`prefix_test` 里写死的「平台状态」——标记改成 `<system-reminder>` 之后它们红在与改动无关的地方，改成跟着 `BLOCK_HEADER` 常量走。**两端门禁通过**（后端 1353 条 + 前端 274 条）。**中途有一轮全量红了 24 条 api 用例（含 3 条 fixture ERROR），如实记在这里** —— 单跑、单文件跑、以及紧接着的整轮重跑都是全绿（1353 passed），判定为环境偶发（这些用例要真 Postgres 与真 Redis），不是本次改动的确定性失败 |
| **十** A9 工具结果预算 | 2026-08-19 | ⚠️ **配好了但没生效，真正跑通在步骤十三**。~~**先量后调**：从首轮结果按「未命中 token ÷ 工具调用次数」估单次工具输出规模 —— 九道题在 312–701，最贵的 E03 约 13856，**而框架默认阈值 20000 正好卡在这两类中间**~~ —— **这两处都错了**：13856 是拿「未命中 token ÷ 工具调用次数」推的，而那个分子绝大部分是历史累积，直接量事件流最大只有 **1820 字符**；「阈值卡在中间」的归因更错，把阈值降到下限 100 卸载次数照样是 0（真因见步骤十三）。**这一条当时就该记「未验」而不是「完成」** —— 阈值配下去了，而「配下去了」与「真的拦住了」是两件事，规矩写在本表抬头。阈值定 `TOOL_RESULT_EVICT_TOKEN = 4_000`（正常上限的约六倍），走 `Settings.agent_tool_result_evict_token` 注入。装配走**按名替换**（与压缩同一条路，`name == "FilesystemMiddleware"`）；**替换会丢掉基础栈传的两个参数，逐一确认过可以不传**：`custom_tool_descriptions` 取自模型 profile 而 DeepSeek 的是空的（实测 `tool_description_overrides == {}`）、`_permissions` 平台从不传（删文件的确认走 `interrupt_on`）—— 这两条写进了 `create_offloader` 的 docstring，换模型时要重看。主图与子图两处都配上了 |
| **七** 改造后对比 | 2026-08-19 | ✅ 完成，**而结论是负面的 —— 这正是尺子该照出来的**。**P13⑥ 噪声带（本期最重要的一个数）**：10 题 × 3 副本，cache 命中率极差**中位数 0.059**（最大 0.596）、未命中 token 相对极差**中位数 32.6%**、轮数极差中位数 1.5。**任何小于 6 个百分点的命中率改进都在噪声里** —— 这条以后每次对比都要先看。**P13⑤**：改造后 10 题里 8 题命中率下降、8 题 token 上升，5 题超出噪声带（4 题变差、1 题变好）。**真因是尾部块本身**：它每轮都变，而下一轮的历史在同一位置换成了别的内容，于是缓存匹配到那里就断了 —— **A6/A8 当初「挂在最后一条消息之后，缓存前缀完整保留」这个假设不成立**。**按数据改了设计**：每节只在有话说时才开口（步数节走过半程才说、装包节没装过就不说），寻常轮次整块不注入。复验三道变化最大的题：E09 从 0.717 回到 **0.963**（基线 0.939）、token 25400 → 2891；E10 从 0.889 回到 **0.960**；E01 部分回升。**P13⑦ probe_coverage 75%**（9/12）—— **压缩三个副本一次都没触发**，阈值 40000 对这批题仍然够不着，那一栏记「未验」不记「过」，`CONTEXT_TRIGGER_TOKEN` 因此仍是观察项 |
| **八** judge 与判分方差 | 2026-08-19 | ✅ 完成。rubric 托管在 Langfuse（`judge-rubric` v1，`production` 标签），版本号跟着结果落盘。**P13⑧ 判分噪声带：同题三副本极差最大 1 分、平均 0.40 分**（5 分制）—— 方差压得住，但相对于现实中能拿到的改进幅度仍然偏大，**因此 [D5](./P13-decision.md) 的处置是先试 C7 的 pairwise，不是直接用绝对打分**。**补判分时尺子又错了一次**：多轮题我拿第一轮的提问配最后一轮的答复喂给 judge，E08 因此得了 1 分（其余 4–5），而它的客观判据是过的 —— 修正配对后回到 5 分。**顺带看到判分方差的直接证据**：E02、E06 是单轮题、配对逻辑没变，重判一次分数就从 4 变成 5 |
| **九** 收尾 | 2026-08-19 | ✅ 完成。**P13①② 已写进 `verify.sh` 并在脚本里真跑通**（判据总数 62 → **64**；§4.1 调整过：P13③ 由 P13② 蕴含、P13④ 归单测）。**全量回归跑了一轮**（`SKIP_HOSTILE=1`，破坏性四条要交互式 sudo，无 tty 跑不了）：**62 条通过、1 条未过、1 条未验**。未过的是 **P3① 的 index 校验前置**——「让 agent 删一个不存在的文件」那次它没去调 delete，于是 run 停不到审批上。**单独重跑三次全部停住**，判定为偶发，不是本期改动打穿的（尾部块在这种一两轮的短 run 里根本不开口）；**但这一轮它确实红了，如实记在这里**。未验的是 P1①（`SKIP_HOSTILE=1`）。**标注队列已建**：`cjk_ok`(BOOLEAN) / `pass_fail`(CATEGORICAL) / `open_coding` 三项 score config 一次建全，再建队列 `zuel-eval-人工抽检` —— 队列建了不能改也不能删，事后想加一项只能另开一个 |
| **六** A2 前缀审计 + A8 尾部注入 | 2026-08-19 | ✅ 完成。**A2 的审计结论是「前缀本来就干净」**：`FilesystemMiddleware` 注入的是静态 prose 与 host-path 路由、`SkillsMiddleware` 注入的是 skill 列表，**都不含文件列表这类每轮会变的东西**；加上 §2.2 那条（DeepSeek 吃不到任何 prompt-caching 中间件），A2 能做的只剩「别把它弄脏」。因此**规则钉成了测试**（`prefix_test.py`）：两轮之间系统提示词必须逐字节相同、动态块必须出现在最后一条消息里而不在系统提示词里、块不许堆进历史。A8 落地为 `TailContextMiddleware`（一个块、内部分节、超预算从后往前丢整节、某节渲染炸了不拖垮整次分析），本期填了环境状态的两节：**已用轮数/上限**与**本会话已装的包**（都从 messages 直接算，零额外 IO）。**真实图测试逼出一个必答题**：只实现同步 `wrap_model_call` 时单测全绿，而平台一路走 `astream` —— 真图一跑就 `NotImplementedError`，异步那支必须一起写。**P13④ 由单测覆盖**（位置、不进历史、预算三条），不进 `verify.sh`：块长什么样只有模型看得见，事件流里看不到，真跑判据验不了它 |
| **五** A1 上下文上限与阈值 | 2026-08-19 | ✅ 完成，**三条判据都真跑验过**。**探针推翻了决策文档的怀疑**：第一发就打 profile 声称的 100 万 token，**过了** —— 模型真吃得下百万上下文，profile 没骗人。压缩确实从未生效过，但真因换了：**85 万那条线在真实使用里根本够不着**（首轮实测单轮历史峰值才 21k）。阈值改由平台定（`CONTEXT_TRIGGER_TOKEN = 40_000`，取实测峰值两倍），主图按名替换、子图直接构造，两处都钉。**中途栽了两次，都值得记**：①**只钉 trigger 不管 keep 等于不压** —— 类默认 `("messages", 20)` 保留最近二十条，而一次分析的历史就十几条，「没有东西可折叠」，token 再多也不触发；②**keep 写成独立常量会自相矛盾** —— 验收时把 trigger 调到 2000 而 keep 还是 8000，保留的比触发线还多，逻辑上永远不可能压。改成按 trigger 比例联动。**P13①**：步数上限配成 4 → `status=failed`、`error_code=RECURSION_LIMIT`，不再混进 INTERNAL。**P13②**：同一道 E01，阈值 2000 压 **4 次**、阈值 40000 压 **0 次**，两向都验到，且顺带量出压缩的代价（cache 命中 0.901→0.823、未命中 token 4045→5798）。**P13③**：平台那份真的替换掉了默认份 —— 用行为证明（免费的假模型探针在平台装配路径下连压四轮）。**验收过程本身还栽了一跤**：我用裸 `docker compose --force-recreate worker` 绕过 Makefile，`SANDBOX_QUOTA_DEVICE` 没传进去，broker 的设备映射回落成 `/dev/null`，配额 fail-closed，症状是建会话 500 —— CLAUDE.md 那句「不要绕过 Makefile 单独调工具」说的正是这个。`.env` 已还原，与验收前一字不差 |
| **四** 基线首轮 | 2026-08-19 | ✅ 完成（单副本 10 题，噪声带留给三副本那轮）。**10/10 全过**。**probe_coverage 3/4**：多轮记忆 ✓、装包 ✓、审批闸门 ✓、**压缩 ✗ 一次都没触发** —— 而这不是题造软了，是**被测系统的配置**：阈值 85 万 token 谁也够不着，正是 A1 要修的那件事，改完再看这道题。**runner 自己先红了一次**：E05 报「未过」，真因是我批准时只发了 `index:0`，而平台要求决策覆盖全部 2 个待确认调用 —— **那个红看着像 agent 没做好**。修完单跑 E05 就过（撞闸门 1 次、产物有、临时文件删净），并补了 3 条 MockTransport 测试。**开放编码的第一个发现**：同样是「过」，E03 烧掉 124704 个未命中 token、cache 命中率 0.41，而 E10 只有 682、命中率 0.96 —— **成本差 20 倍**，差在工具输出的大小（它把整段数据序列打印出来分析），不在轮数。**二值判据完全看不出这个差异**，而成本正是本期要优化的维度。**据此判断：不重造题**（§7 第 2 条的两个条件都不成立 —— 主判据是连续量不是二值，且题目在成本维度上有 20 倍分辨度） |
| **三** runner | 2026-08-19 | ✅ 完成。`create_account.sh`（专用评估账号，与 verify.sh 同一套造号做法）、`metric.py`（事件流 → 事实 + 判据评估，**12 条单测正反两向**）、`run_eval.py`（走真实链路 + experiment 上报 + 本地落盘）、`--dry-run` 自检（挡「跑到第 8 题才发现少个数据文件」，实测 10 题 0 问题）。**修掉一处开工前就埋着的错**：`client.login` 发的是 `username`，而平台认的是 `name` —— 不修就是 422，且报错指向请求体不指向账号。**冒烟真跑了一道锚题**：判据过、本地 JSON 落盘、Langfuse dataset run 建出来，`/v3/scores` 读回 item 级 7 项 + run 级 4 项。**顺带把 C6 那条「未验」验了**：`experiment-items?fields=io` 真的带回 input/output/expectedOutput/scores/traceId。**读回来的数照出我自己代码里两处假红**：可判 probe 为空时 `probe_coverage` 报 0（会被读成「一题都没触发」）、单副本时 `cache_hit_rate_spread` 报 0（会被读成「毫无波动」）——都改成算不出来就不发，并各补一条测试 |
| **二** 压缩采集通路 | 2026-08-19 | ✅ 完成。**开工前先探针实测**：压缩发生那一轮，`_summarization_event` 确实出现在 `updates` 流的 model 节点里（私有 state 字段没被过滤）；把 trigger 调到不触发，同一道题的 update 里这个键就消失 —— **正反两向都验过**，因此「有这个键 = 这一轮压了一次」成立。采集通路因此接在 mapper，**不需要自定义中间件、也用不上 custom 通道**。新增 `EventType.COMPACTION` + `CompactionData`（`cutoff_index` / `file_path`），前端同步订阅名单、支持名单、载荷类型与校验。**前端的穷尽 switch 逼出了一个必答题**：`eventReducer` 少一个分支就编译不过，于是压缩在教师那侧渲染成一条 info 提示（不是 error —— 它是正常机制，记成错误会让人白紧张）。**打穿了三条既有用例**：①「model 节点没有 messages 要告警」被我一刀切静默了，已把豁免收窄到只对带压缩痕迹的更新；②③ 两处硬编码「15 个事件名」的断言，改成跟着 `RUN_EVENT_NAMES` 走，以后加事件不会再红在这里。两端门禁全绿（后端 + 前端 274 条） |
| **一** 递归错误码 + 上限可配 | 2026-08-19 | ✅ 完成。`RunErrorCode.RECURSION_LIMIT` 落地，`executor` 在宽捕获**之前**认 `GraphRecursionError`；`RECURSION_LIMIT = 60` 改由 `Settings.agent_recursion_limit` 注入（可配是为了 P13① 能把它调到极小值触发）；前端 `RunErrorCode` 与白名单同步。**先写测试后写实现，两条测试都先红过**。两端门禁全绿（后端 + 前端 269 条），未打穿任何历史用例 |
