"""链路追踪的接线：一个 run 的完整 trace 怎么串起来。

**与 `metric/` 分家。** 那边回答「此刻平台是什么样」，是一屋子仪表盘；这边回答
「**这一个** run 的时间花在哪儿了」，是一条时间轴。两者的采集方式、存储、
以及出问题时看的页面都不一样。

一条完整的 trace 要跨三个进程，其中两处的接法完全不同：

| 跨越 | 怎么串上的 |
|---|---|
| 浏览器 → api | FastAPI 探针自动开根 span |
| api → broker、worker → broker | httpx 探针自动把 traceparent 放进请求头 |
| **api → worker** | **只有这一段要手接** —— 中间隔着一条 Redis 队列，没有请求头可放 |

第三条是这个包存在的主要理由（`context.py`）。漏了它不会报错，只会让每个 run
断成互不相干的两条 trace，而「一个 run 的完整耗时」正好落在断口上。
"""
